import ast                      # Parse expressions into a syntax tree (safe alternative to eval)
import operator                 # ???Operators mapping (add, mul, etc.)
import math                     # Give me math! Or give me death.
import json                     # Save history
import os                       # filesystem checks
from datetime import datetime   # Timestamp history entrie